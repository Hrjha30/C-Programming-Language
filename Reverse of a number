// Reverse of a number

#include<stdio.h>
int main()
{int num,rev=0,remainder,i=1;
printf("Enter the number : ");
scanf("%d",&num);
while(num!=0)
{remainder=num%10;
rev=rev*10+remainder;
num=num/10;
}
printf("Reverse of the number %d is : %d",num,rev);
return 0;
}
