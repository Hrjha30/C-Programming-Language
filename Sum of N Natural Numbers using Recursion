// Sum of N Natural Numbers using Recursion

#include<stdio.h>
int sumofn(int);
int main()
{int num,result;
printf("Enter the number : ");
scanf("%d",&num);
result=sumofn(num);
printf("Sum is : %d",result);
return 0;
}
int sumofn(int n)
{if(n>1)
return n + sumofn(n-1);
else
return 1;}
