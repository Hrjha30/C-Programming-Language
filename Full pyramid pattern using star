// Full pyramid pattern using star

#include<stdio.h>
int main()
{int i,j,num,k;
printf("Enter the number of rows : ");
scanf("%d",&num);
for(i=1;i<=num;i++)
{for(j=1;j<=num-i;j++)
{
printf(" ");}
for(k=1;k<=i;k++)
printf(" *");
printf("\n");
}
return 0;
}
